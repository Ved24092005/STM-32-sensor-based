# STM-32-sensor-based
This project implements a traffic light control system using an STM32 microcontroller. The STM32 controls the traffic signal LEDs according to a predefined sequence and can use sensors to detect vehicle presence or traffic density.
